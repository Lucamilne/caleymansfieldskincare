import React from "react";
import { Container } from "react-bootstrap"

class Products extends React.Component {
    render() {
        return (
            <Container className="pl-0 pr-0" id="products">
                <h1 className="title">Treatments</h1>
            </Container>
        )
    }
}

export default Products;